<% include _header %>
<% if(user === undefined){ %>
    <% include _navbar_guest.ejs %>
<% }else{ %>
    <% include _navbar_auth.ejs %>
<% } %>
<div class="container">
    <table class="highlight">
        <thead>
        <tr>
            <th>Book</th>
            <th>Author</th>
            <th>Library</th>
            <th>Status</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        <% booklist.forEach(function (book) { %>
            <tr onclick="document.location='/book/<%= book.name%>'">
                <td><%= book.name %></td>
                <td><%= book.author %></td>
<!--                <td><%= book.year %></td>-->
<!--                <td><%= book.page_count %></td>-->
                <td ><%= library[book.library_id - 1].name %></td>
                <td>
                    <p>
                        <label>
                            <input type="checkbox" disabled="disabled" <%= book.status ? "checked " : "" %> />
                            <span><%= book.status ? "In Use":"In Library" %></span>
                        </label>
                    </p>
                </td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</div>

<% include _footer %>